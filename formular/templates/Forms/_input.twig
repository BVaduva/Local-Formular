<label for="{{ id }}" class="form-label">{{ label }}</label>
<input type="{{ type }}" name="{{ name }}" class="form-control" id="{{ id }}" placeholder="{{ placeholder|default('') }}" value="{{ value|default('') }}" {% if required %} required {% endif %}>
